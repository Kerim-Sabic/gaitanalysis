# Horalix Gait AI — backend dependencies
# Core API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20

# Scientific / CV core (REAL pipeline)
numpy==2.2.1
scipy==1.15.0
opencv-python-headless==4.11.0.86

# Reporting
reportlab==4.2.5

# --- Optional heavy models (graceful fallback if missing) ---
# mediapipe==0.10.21        # real on-device 2D pose (CPU). pip install mediapipe
# torch / mmpose / mmcv     # see docs/architecture.md for RTMPose/RTMW3D setup
# segment-anything-2        # SAM 2 person masking
