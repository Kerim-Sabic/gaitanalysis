# URL of the FastAPI backend. The Next.js rewrite proxies /api/* -> here.
NEXT_PUBLIC_API_URL=http://localhost:8000

# On Netlify, set API_URL in the Netlify environment instead. It is server-only
# and should point to the publicly reachable FastAPI deployment.
# API_URL=https://api.example.com
